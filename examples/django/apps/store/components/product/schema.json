{
  "title": "Product Card",
  "description": "Displays a product with image, name, price, and description. Supports list and detail modes.",
  "version": "1.1.0",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Product name"
    },
    "price": {
      "type": "number",
      "description": "Product price in USD"
    },
    "description": {
      "type": "string",
      "description": "Product description"
    },
    "imageUrl": {
      "type": "string",
      "description": "URL to product image"
    },
    "url": {
      "type": "string",
      "description": "URL to product detail page (makes card clickable)"
    },
    "mode": {
      "type": "string",
      "enum": ["list", "detail"],
      "default": "detail",
      "description": "Display mode: 'list' for compact cards, 'detail' for full view"
    }
  },
  "required": ["name", "price"]
}
